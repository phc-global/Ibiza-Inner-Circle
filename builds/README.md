# builds/

This is where the work goes. When you ask Claude to build a landing page, a funnel,
an SMS flow, or a spec, it writes the real files here (e.g. `builds/my-landing-page/`).

Nothing to do — just ask `claude` to build something and watch this folder fill up.
